# RAG 智能问答系统 - 安装指南

## 快速开始

### 方式一：使用 PowerShell 脚本（推荐）

1. 右键点击 `download_ollama.ps1` 文件
2. 选择"使用 PowerShell 运行"
3. 等待下载完成
4. 运行安装程序

### 方式二：手动下载

1. 打开浏览器访问：https://ollama.com/download
2. 下载 Windows 版本的安装程序（OllamaSetup.exe）
3. 运行安装程序

## 详细安装步骤

### 第一步：安装 Ollama

#### Windows 安装

1. 下载 OllamaSetup.exe
2. 双击运行安装程序
3. 按照安装向导完成安装
4. 安装完成后，Ollama 会自动在后台启动

#### 验证安装

打开命令提示符（CMD）或 PowerShell，运行：

```bash
ollama --version
```

如果显示版本号，说明安装成功！

### 第二步：下载模型

打开命令提示符（CMD）或 PowerShell，运行以下命令：

#### 1. 下载嵌入模型（必需）

```bash
ollama pull nomic-embed-text
```

#### 2. 下载大语言模型（选择一个）

**推荐：DeepSeek-R1 7B**
```bash
ollama pull deepseek-r1:7b
```

**备选：Qwen2 7B**
```bash
ollama pull qwen2:7b
```

**轻量版：Qwen2 1.5B（适合低配置机器）**
```bash
ollama pull qwen2:1.5b
```

### 第三步：启动应用

#### 启动完整版本

```bash
streamlit run app.py
```

#### 启动演示版本（无需 Ollama）

```bash
streamlit run app_demo.py
```

## 常见问题

### Q: 下载模型很慢怎么办？

A: 可以尝试：
- 配置 Ollama 使用镜像源
- 在网络较好的时候下载
- 使用更小的模型（如 qwen2:1.5b）

### Q: Ollama 服务无法启动？

A: 检查：
1. Ollama 是否已安装
2. 端口 11434 是否被占用
3. 重启电脑试试

### Q: 内存不够怎么办？

A: 建议：
- 使用更小的模型（qwen2:1.5b）
- 关闭其他占用内存的程序
- 增加系统虚拟内存

### Q: 如何验证 Ollama 服务是否正常？

A: 运行：

```bash
ollama list
```

如果显示已下载的模型列表，说明服务正常。

## 系统要求

- **操作系统**：Windows 10/11
- **Python**：3.9 或更高版本
- **内存**：
  - 推荐：16GB 或更高（7B 模型）
  - 最低：8GB（1.5B 模型）
- **硬盘**：至少 10GB 可用空间

## 项目文件说明

```
RAG-QA-System/
├── app.py                  # 完整版本应用
├── app_demo.py             # 演示版本应用
├── rag_chain.py            # RAG 问答链核心
├── knowledge_base.py       # 知识库管理
├── test_ollama.py          # Ollama 测试脚本
├── download_ollama.ps1     # Ollama 下载脚本
├── requirements.txt        # 依赖列表
├── build.spec             # 打包配置
├── .gitignore            # Git 忽略配置
├── README.md             # 项目说明
├── 安装指南.md           # 本文件
├── docs/
│   └── 自然语言处理简介.txt  # 示例文档
└── chroma_db/            # 向量数据库（自动生成）
```

## 使用演示版本

如果暂时不想安装 Ollama，可以使用演示版本：

1. 运行：`streamlit run app_demo.py`
2. 在浏览器中打开 http://localhost:8501
3. 上传文档并体验界面功能

注意：演示版本没有真实的 AI 回复功能，仅用于展示界面。

## 获取帮助

如果遇到问题：
1. 查看 GitHub 仓库的 Issues
2. 检查 Ollama 官方文档：https://ollama.com/docs
3. 查看 Streamlit 官方文档：https://docs.streamlit.io

---

祝你使用愉快！ 🚀